hostname {{ hostname }}
ip cef

interface {{ interface_name }}
 description link to PE
 ip address {{ ip }} 255.255.255.0
 no shutdown

router bgp {{ as_number }}
 bgp log-neighbor-changes
 redistribute connected
 neighbor {{ pe_ip }} remote-as {{ backbone_as }}
